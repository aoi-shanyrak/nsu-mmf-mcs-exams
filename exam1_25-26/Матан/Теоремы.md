### 1. о точной верхней\нижней границе
докво:
1. множество границ и аксиома непрерывности

### 2. о промежуточной последовательности
докво:
1. когда для обоих выполняется

### 3. вейерштрассе о монотонной последовательности
докво:
1. M точная вг
2. m - e < xn <= xne < m

### 4. критерий Гейне о существовании предела функции
докво: 
1. (=>) для любой xn -> x0 выполнено 
2. (<=) (от противного) cущ для которого не выполнено (по коши)
3. строим такую посл что сходится к x0 и она не сходится к A (противоречие)
### 5. о частичных пределах (больцано-вейерштрассе)
докво:
1. посл супремумов
2. xn огр => hm огр и убывает => hm -> b
3. xnk <= hnk (любой част предел меньше)
4. сущ hm1 :   b <= hm1 < b + 1
5. сущ n1 >= m1 :  b - 1 <= hm1 - 1 < xn1 <= hm1 < b + 1
6. по индукции b - 1/j < xnj < b + 1/j      => xnk -> b

### 6. критерий коши
докво: 
1. (=>) |xn - xm|<=|xn - a|+|xm - a|
2. (<=) огр  xn в (xm -e, xm + e)
3. |xn - a| <= |xn - xnk | + |xnk - a| = e

### 9. больцано-коши о прожуточных значениях непрерывной функции
докво: 
1. inf < y < sup
2. f(ai) < y < f(bi)  индукция
3. посл an bn -> f(x) = y

### 10. вейерштрассе об экстремумах непрерывной функции
докво:
1. yn -> m = sup
2. yn = f(xn)
3. xn в [a,b] => xn - огр
4. xnk -> q
5. a <= xnk <= b   =>  a <= q <= b
6. ynk = f(xnk) -> f(q)  => m = f(q)

### 12. кантора о непрерывной функции на отрезке
докво:
1. |f(x) - f(y)| <= w(|x-y|)
2. (от против) w !-> 0
3. xnk -> c   ynk -> c   => w() -> 0

### 13. правило дифференцирования композиции функции
докво:
1. по разлож лейбница

### 14. о производной обратной функции
докво: по построению

### 15. ролля
докво:
1. сущ max значение
2. по теореме ферма есть f'(c) = 0

### 16. лагранжа о среднем
докво: 
1. g(x) = f(x) - ((f(b)-f(a))/(b - a))(x - a)
2. g(b) = g(a) = f(a)
3. по теореме ролля сущ g'(c) = 0

### 17. признак выпуклости
докво: 
1. (=>) x1 = a < x1 + t = b < x2 = c
2. b - a < c - a < c - b
3. f'(x1) <= f'(x2)
4. (=>) параметрически задать xt
5. по теореме лагранжа и выразить xt    ( f'(e1) < f'(e2) )

### 18. асиптотическая формула тейлора и остаточный член пеано
докво:
1. разложение лейбница для функции
2. (индукция) пусть верно для предыдущего
3. g(x) = f(x) - T(x)
4. g(x0) = 0   g'(x) = o(x - x0)^(n-1)
5. (по лагранжу) сущ с: g'(c) = (g(x)-g(x0))/(x-x0)
6. подставляем и показываем что lim (g(x)/(x-x0)^n) = 0

### 19. формула тейлора и остаточный член в лагранжа
докво:
1. строим функцию g(x) = f(x) - Tn(x) - (x - x0)^(n+1) (f(x1) - Tn(x1))/(x1 - x0)^(n+1)
2. по индукции все производные равны нулю 
3. получаем нужную формулу

### 20. экспонента
докво:
1. для zn -> 0
2. неравенство бернулли обычное
3. неравенство бернулли для степени -1
4. зажимаем неравенство и предел равен 1
5. представляем xn = a + zn
6. как произведение устремляем

### 21. мини доква на пределы
докво:
корень n из n: 
	по биному ньютона
логарифм и степенная:
	представить как е в степени и заменить переменную
отношения 
	как отношения соседних элементов
факториал
	оценка по половине множителей