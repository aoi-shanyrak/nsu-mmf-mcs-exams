# Линейные и аффинные отображения

## Линейные отображения векторных пространств

Пусть V и W — векторные пространства над одним полем F.

**Линейное отображение** — это отображение f: V → W, удовлетворяющее условиям:
1. f(u + v) = f(u) + f(v) для любых u, v ∈ V
2. f(λ·v) = λ·f(v) для любого λ ∈ F и любого v ∈ V

**Свойства линейных отображений:**
1. f(0_V) = 0_W (образ нуля есть ноль)
2. f(-v) = -f(v)
3. f(α₁v₁ + ... + αₖvₖ) = α₁f(v₁) + ... + αₖf(vₖ) для любых αᵢ ∈ F, vᵢ ∈ V
4. Образ линейной комбинации равен линейной комбинации образов

---

## Ядро и образ линейного отображения

Для линейного отображения f: V → W:

**Ядро** линейного отображения — множество Ker f = {v ∈ V | f(v) = 0_W}
**Образ** линейного отображения — множество Im f = {w ∈ W | ∃v ∈ V: f(v) = w}

**Свойства:**
1. Ker f — подпространство в V
2. Im f — подпространство в W
3. f инъективно (разные векторы имеют разные образы) ⇔ Ker f = {0}

---

## Формулы линейного отображения в координатах

Пусть V и W — конечномерные пространства с базисами:
𝓑_V = (v₁, ..., vₙ) — базис V
𝓑_W = (w₁, ..., wₘ) — базис W

Линейное отображение f: V → W задается матрицей A размера m×n такой, что для любого вектора x ∈ V с координатным столбцом X = (x₁, ..., xₙ)ᵀ в базисе 𝓑_V координатный столбец Y = (y₁, ..., yₘ)ᵀ образа f(x) в базисе 𝓑_W вычисляется по формуле:
Y = A·X

Матрица A строится так: j-й столбец матрицы A — это координатный столбец вектора f(vⱼ) в базисе 𝓑_W.

---

## Аффинные отображения

**Аффинное отображение** — отображение φ: V → W между векторными пространствами, которое можно представить в виде:
φ(v) = f(v) + b
где f: V → W — линейное отображение, b ∈ W — фиксированный вектор.

**Индуцированное линейное отображение:** Для аффинного отображения φ(v) = f(v) + b линейная часть f называется индуцированным линейным отображением.

---

## Формулы аффинного отображения в координатах

Пусть V и W — векторные пространства с фиксированными системами координат (начало в точке O для V и в точке O' для W).

Аффинное отображение φ: V → W задается формулой:
Y = A·X + B
где:
- X — координатный столбец точки в V
- Y — координатный столбец ее образа в W
- A — матрица линейной части (размера m×n для dim V = n, dim W = m)
- B — координатный столбец вектора сдвига

**Формулы аффинного отображения при переходе к другой системе координат:** При замене координат в V матрицей C, а в W матрицей D, матрица аффинного отображения преобразуется по формуле:
A' = D⁻¹·A·C
B' = D⁻¹·B

---

## Аффинные преобразования

**Аффинное преобразование** — это обратимое аффинное отображение пространства на себя.

**Неподвижные точки аффинного преобразования** — точки, удовлетворяющие уравнению:
φ(x) = x, или A·X + B = X

Классификация аффинных преобразований плоскости и пространства:
1. **Параллельный перенос:** φ(x) = x + a, где A = E (единичная матрица), B = a
   - Нет неподвижных точек (если a ≠ 0)
   - Сохраняет расстояния и направления
2. **Гомотетия с коэффициентом k:** φ(x) = k·x + (1-k)·c, где c — центр гомотетии
   - Единственная неподвижная точка — центр c
   - Увеличивает или уменьшает расстояния в |k| раз
3. **Симметрия относительно точки (центральная симметрия):** φ(x) = -x + 2c
   - Частный случай гомотетии с k = -1
   - Центр c — единственная неподвижная точка
4. **Симметрия относительно прямой или плоскости:** φ(x) = f(x) + b, где f — ортогональное отражение

---

## Изометрические отображения

**Изометрическое отображение** (изометрия) — отображение φ: V → W между евклидовыми пространствами, сохраняющее расстояния:
||φ(x) - φ(y)|| = ||x - y|| для любых x, y ∈ V

**Ортогональная матрица изометрического отображения:** Матрица A линейной части изометрического отображения удовлетворяет условию:
Aᵀ·A = E (или A⁻¹ = Aᵀ)

**Свойства ортогональных матриц:**
1. |det A| = 1
2. Строки и столбцы образуют ортонормированные системы векторов
3. Сохраняет скалярное произведение: (A·x, A·y) = (x, y)

---

## Группы O(n) и SO(n)

**O(n)** — группа ортогональных матриц размера n×n:
O(n) = {A ∈ Mₙ(ℝ) | Aᵀ·A = E}

**SO(n)** — специальная ортогональная группа (группа вращений):
SO(n) = {A ∈ O(n) | det A = 1}

**Свойства:**
1. O(n) — группа относительно умножения матриц
2. SO(n) — подгруппа O(n) индекса 2
3. Элементы SO(2) — повороты плоскости
4. Элементы O(2)\SO(2) — отражения

---

## Поворот

**Поворот плоскости** — изометрическое преобразование с одной неподвижной точкой (центром поворота) и определителем линейной части равным 1.

В ортонормированном базисе поворот на угол φ задается матрицей:
[cos φ  -sin φ]
[sin φ   cos φ]

**Свойства поворотов:**
1. Композиция двух поворотов на углы φ и ψ — поворот на угол φ+ψ
2. Поворот сохраняет ориентацию (det = 1)
3. Группа поворотов плоскости вокруг фиксированной точки изоморфна SO(2)

**Поворот в пространстве:** Поворот в ℝ³ вокруг оси, заданной единичным вектором u, на угол φ. Матрица такого поворота в ортонормированном базисе, где третий базисный вектор совпадает с u, имеет вид:
[cos φ  -sin φ  0]
[sin φ   cos φ  0]
[  0       0    1]