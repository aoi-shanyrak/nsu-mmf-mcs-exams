### **1. Аксиомы векторного пространства**

**Определение:** Пусть задано множество `V` (элементы — векторы) и поле `F` (элементы — скаляры, например, ℝ или ℂ). Заданы две операции:
1. **Сложение векторов:** `V × V → V`, обозначается `u + v`.
2. **Умножение на скаляр:** `F × V → V`, обозначается `λv`.

`V` называется **векторным (линейным) пространством** над полем `F`, если выполняются следующие 8 аксиом:
- **Для сложения:**
    1. `u + v = v + u` (коммутативность).
    2. `(u + v) + w = u + (v + w)` (ассоциативность).
    3. Существует **нулевой вектор** `0 ∈ V`: `v + 0 = v` для любого `v ∈ V`.
    4. Для каждого `v ∈ V` существует **противоположный вектор** `-v ∈ V`: `v + (-v) = 0`.
- **Для умножения на скаляр:**  
    5. `1 * v = v` (умножение на единичный скаляр поля).  
    6. `α(βv) = (αβ)v` для всех `α, β ∈ F` (ассоциативность).
- **Дистрибутивность:**  
    7. `(α + β)v = αv + βv` (дистрибутивность относительно сложения скаляров).  
    8. `α(u + v) = αu + αv` (дистрибутивность относительно сложения векторов).    

---

### **2. Линейно зависимые и линейно независимые векторы**

**Определение:** Система векторов `{v₁, v₂, ..., vₖ}` в пространстве `V` называется **линейно зависимой**, если существуют скаляры `α₁, α₂, ..., αₖ ∈ F`, **не все равные нулю**, такие что:  
`α₁v₁ + α₂v₂ + ... + αₖvₖ = 0`.

**Определение:** Система векторов `{v₁, v₂, ..., vₖ}` называется **линейно независимой**, если равенство `α₁v₁ + α₂v₂ + ... + αₖvₖ = 0` выполняется **только при** `α₁ = α₂ = ... = αₖ = 0`.

---

### **3. Размерность векторного пространства и его базис**

**Определение (Базис):** **Базисом** векторного пространства `V` называется такая система векторов `{e₁, e₂, ..., eₙ}`, которая:
1. **Линейно независима.**
2. **Порождает** (или является полной в) `V` (любой вектор `v ∈ V` линейно выражается через векторы базиса).

**Определение (Размерность):** **Размерностью** векторного пространства `V` называется количество векторов в его базисе. Обозначается `dim V`. Если базис содержит бесконечно много векторов, пространство называется **бесконечномерным**.

---

### **4. Координаты векторов**

**Определение:** Пусть `𝓑 = (e₁, e₂, ..., eₙ)` — **упорядоченный базис** пространства `V`. Если вектор `v` разлагается в этом базисе как `v = x₁e₁ + x₂e₂ + ... + xₙeₙ`, то скаляры `(x₁, x₂, ..., xₙ)` называются **координатами** вектора `v` в базисе `𝓑`.  
Обозначение: `[v]_{𝓑} = (x₁, x₂, ..., xₙ)ᵀ` (часто записывают как столбец).

---

### **5. Изоморфизм векторных пространств**

**Определение:** Векторные пространства `V` и `W` над одним полем `F` называются **изоморфными** (`V ≅ W`), если существует биективное (взаимно однозначное) отображение `φ: V → W`, которое является **линейным**, т.е.:
1. `φ(u + v) = φ(u) + φ(v)` для всех `u, v ∈ V`.
2. `φ(λv) = λ φ(v)` для всех `λ ∈ F`, `v ∈ V`.  
    Такое отображение `φ` называется **изоморфизмом**.

**Координатный изоморфизм:**

> Пусть `V` — конечномерное пространство размерности `n` над полем `F`, и `𝓑` — его фиксированный базис. Тогда отображение  
> `φ: V → Fⁿ`, заданное правилом `φ(v) = [v]_{𝓑}` (сопоставление вектору его столбца координат),  
> является изоморфизмом.  
> **Следствие:** Любое n-мерное пространство над полем `F` изоморфно арифметическому пространству `Fⁿ`.

---

### **6. Векторные подпространства**

**Определение:** Непустое подмножество `U` векторного пространства `V` называется **подпространством** (`U ≤ V`), если оно само является векторным пространством относительно операций сложения и умножения на скаляр, определенных в `V`.

**Критерий подпространства:** `U` — подпространство `V` **тогда и только тогда**, когда:
1. `0 ∈ U`.
2. Для любых `u₁, u₂ ∈ U` их сумма `u₁ + u₂ ∈ U` (замкнутость относительно сложения).
3. Для любого `u ∈ U` и любого `λ ∈ F` вектор `λu ∈ U` (замкнутость относительно умножения на скаляр).

---

### **7. Пересечение и сумма векторных пространств**

**Определение (Пересечение):** Пересечение `U ∩ W` двух подпространств `U` и `W` пространства `V` — это множество векторов, принадлежащих одновременно и `U`, и `W`. `U ∩ W` всегда является подпространством.

**Определение (Сумма):** Суммой `U + W` двух подпространств называется множество всех векторов вида `u + w`, где `u ∈ U`, `w ∈ W`.  
`U + W = { v ∈ V | v = u + w, u ∈ U, w ∈ W }`. Это также подпространство в `V`.

---

### **8. Прямая сумма векторных подпространств**

**Определение:** Сумма подпространств `U + W` называется **прямой**, если каждый вектор этой суммы **единственным образом** представляется в виде `u + w`. Обозначается: `U ⊕ W`.

**Критерий прямой суммы:** Следующие условия эквивалентны:
1. `U + W = U ⊕ W`.
2. `U ∩ W = {0}` (пересечение тривиально).
3. `dim(U + W) = dim U + dim W`.

---

### **9. Линейная оболочка**

**Определение:** **Линейной оболочкой** системы векторов `S = {v₁, ..., vₖ}` называется множество всех линейных комбинаций этих векторов:  
`L(S) = { λ₁v₁ + ... + λₖvₖ | λᵢ ∈ F }`.  
Обозначается также `span(S)` или `<S>`.

**Свойства линейных оболочек:**
1. `L(S)` — наименьшее подпространство, содержащее все векторы системы `S`.
2. Если `S₁ ⊆ S₂`, то `L(S₁) ⊆ L(S₂)`.
3. `L(S₁ ∪ S₂) = L(S₁) + L(S₂)`.
4. Система векторов `S` порождает пространство `V` **тогда и только тогда**, когда `L(S) = V`.

